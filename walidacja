    if ( getSheetType(PODSUMOWANIE).getRange(cellSuma).getValue() == 0 &&
         getSheetType(PODSUMOWANIE).getRange(cellWplata).getValue() == ''){
      // znak, że wszystko zostało zwrocone, nic sprzedane wiec bierzemy kwote brutto
      getSheetType(PODSUMOWANIE).getRange(cellSumaNaSklepie).setValue(modelsList[model][0]*modelsPriceList[model])
    
    } else if (getSheetType(PODSUMOWANIE).getRange(cellSuma).getValue() == 0 &&
               getSheetType(PODSUMOWANIE).getRange(cellWplata).getValue() != ''){
      // znak, że wszystko zostało sprzedane wiec bierzemy kwote netto po 
      
      getSheetType(PODSUMOWANIE).getRange(cellSumaNaSklepie).setValue(-modelsList[model][2]*(modelsPriceList[model]+modelsPriceList[model]*procent))
    
    } else if (getSheetType(PODSUMOWANIE).getRange(cellSuma).getValue() != 0 &&
               getSheetType(PODSUMOWANIE).getRange(cellWplata).getValue() != '' &&){
      // znak, że częśc została sprzedana a częśc nie
      
      netto = -modelsList[model][2]*(modelsPriceList[model]+modelsPriceList[model]*procent)
      brutto = suma*(modelsPriceList[model])
      getSheetType(PODSUMOWANIE).getRange(cellSumaNaSklepie).setValue(netto+brutto)
      
    } else if (getSheetType(PODSUMOWANIE).getRange(cellSuma).getValue() != 0 &&
               getSheetType(PODSUMOWANIE).getRange(cellWplata).getValue() == '' &&){
      // znak, że nic nie zostało sprzedane, ale zostało zwrócone mniej niż dostarczone
      getSheetType(PODSUMOWANIE).getRange(cellSumaNaSklepie).setValue()
    
    }